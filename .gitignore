# macOS
.DS_Store
**/.DS_Store

# Editor / OS
.vscode/
*.swp
*~

# Raw photo originals (kept locally as backup, not deployed)
# Only photos under prototype/images/ are tracked for the live site
/*.jpg
/*.jpeg
/*.png
/*.webp
/*.svg
!/README.png

# Backup of source photos, organized into subfolders by filename pattern
# (logos, named-descriptive, wordpress-dated, phone-uploads, uuid-named, misc)
/originals/

# Raw photo drop folder — full-res GoPro/dive originals (hundreds of MB).
# Optimized versions get committed under prototype/images/ only.
/Pictures ScubaCuba.ca/

# Keep prototype assets
!prototype/**

# Local-only Claude config
.claude/

# Photo classification work file
photo-classification.json

# Logs
*.log
/tmp/

# External symlinks from another project
prototype/assets
prototype/kairo-preview
